<!-- Generated by ensemble-pi generator v1.0.0 | Source: generate-feature-tests.yaml -->
<!-- Command: ensemble:generate-feature-tests | Version: 1.0.0 -->
<!-- Description: Generate Gherkin .feature files and a traceability manifest from a PRD's acceptance criteria -->

# ensemble:generate-feature-tests

> **Mission:** Generate executable-shaped Gherkin .feature files directly from a PRD's acceptance criteria, plus a .prd-trace.json manifest that fingerprints every AC for later drift detection. PRD acceptance criteria are already written in Given/When/Then form, so this is a faithful, deterministic translation rather than a creative authoring step. The output is one .feature file per requirement (REQ-NNN), with traceability tags linking each scenario back to its PRD document id and AC id.

> **Constraints:**
> - This command ONLY generates test artifacts from an existing PRD -- it does not implement features or write step definitions
> - Parsing is deterministic via the prd-cli helper -- DO NOT hand-author or hand-edit .feature files
> - Every acceptance criterion (AC-NNN-M) maps to exactly one Gherkin Scenario; nothing is dropped
> - Acceptance criteria that are not in Given/When/Then form are emitted as @needs-clarification scenarios, never silently skipped
> - Regeneration is idempotent -- unchanged ACs produce byte-identical output

## Phase 1: Generate

### Step 1: Resolve the PRD path

Determine which PRD to generate feature files from

**Actions:**
1. If the user passed a PRD path argument, use it
2. Otherwise look under docs/PRD/ and ask the user which PRD to use if ambiguous

### Step 2: Run the generator

Invoke the deterministic CLI to parse the PRD and write artifacts

**Actions:**
1. Resolve PRD_CLI to first existing path among: ${CLAUDE_PLUGIN_ROOT}/lib/prd-cli.js, packages/product/lib/prd-cli.js. If missing, print error and HALT.
2. Run: node "$PRD_CLI" generate-features <prd-path> --json
3. Pass through --out <dir> (default: features/) and --dry-run if the user requested a preview
4. The CLI writes one <REQ-NNN>.feature per requirement plus a .prd-trace.json manifest under <out>/<prd-stem>/

### Step 3: Report results

Summarize what was generated and surface gaps

**Actions:**
1. Report the output directory, number of feature files, and total scenarios
2. List every AC tagged @needs-clarification (free-form or carrying a [NEEDS CLARIFICATION] marker) so the author can fix the PRD
3. Echo any parser warnings (e.g. requirements with no acceptance criteria)
4. Remind the user to commit the generated features and run /ensemble:check-feature-drift after future PRD edits
