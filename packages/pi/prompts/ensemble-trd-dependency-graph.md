<!-- Generated by ensemble-pi generator v1.0.0 | Source: trd-dependency-graph.yaml -->
<!-- Command: ensemble:trd-dependency-graph | Version: 1.0.0 -->
<!-- Description: Build a dependency graph across TRDs and report likely-duplicate work (overlapping target files) -->

# ensemble:trd-dependency-graph

> **Mission:** Build and emit the TRD-level dependency graph for a set of TRDs, and surface likely-duplicate technical work so the same domain object is not specified in two places. Reuses the existing cross-TRD dependency annotations to derive edges, detects cycles, and renders the graph as JSON, Mermaid (for GitHub / markdown), or Graphviz DOT. The overlap report flags TRD pairs that touch the same target files -- a signal that work should be extracted into one shared (foundational) TRD and referenced rather than duplicated.

> **Constraints:**
> - This command is READ-ONLY -- it analyzes existing TRDs and never edits them
> - Graph construction is deterministic via the trd-graph-cli helper -- DO NOT hand-build the graph or eyeball dependencies
> - Nodes are keyed by the filename-derived slug (unique); document_id is shown as a correlation attribute and duplicate document_ids are reported as warnings, never silently merged
> - Cross-TRD edges come only from existing [depends: <trd-slug>#TRD-NNN] / #PR-N annotations; bare TRD-NNN deps are intra-TRD and excluded

## Phase 1: Analyze

### Step 1: Resolve the TRD set

Determine which TRDs to graph

**Actions:**
1. If the user passed a directory or explicit TRD paths, use them; otherwise default to docs/TRD/

### Step 2: Build the graph

Invoke the deterministic CLI to build and emit the dependency graph

**Actions:**
1. Resolve TRD_GRAPH_CLI to first existing path among: ${CLAUDE_PLUGIN_ROOT}/lib/trd-graph-cli.js, packages/development/lib/trd-graph-cli.js. If missing, print error and HALT.
2. Run: node "$TRD_GRAPH_CLI" graph <dir|paths> --format <json|mermaid|dot>
3. Default to --format mermaid for a human-facing answer (renders in GitHub); use json when the caller needs machine output, dot for Graphviz
4. Interpret exit code: 0 = acyclic, 2 = a dependency cycle was detected (report the cycle)
5. Surface every stderr warning -- especially duplicate document_id collisions and unresolved cross-TRD references

### Step 3: Report duplicate work

Run the overlap report to flag probable duplication

**Actions:**
1. Run: node "$TRD_GRAPH_CLI" overlap <dir|paths>
2. For each flagged pair, note the shared target files and suggest extracting the shared work into one foundational TRD that both reference via [depends: <trd-slug>#TRD-NNN]
3. Present the graph (rendered) plus the cycle and overlap findings; do not modify any TRD
